Refactor Career Page UI:
Change the current Career/Jobs page layout to be much simpler, less cluttered, and realistic for a Freshman university student.

Data Rules:

Remove senior roles like "Software Architect". Use realistic entry/mid-level roles like "Software Engineer", "Site Engineer", "Data Analyst".

Remove USD salaries. Use realistic starting/junior level local salaries (e.g., "6 သိန်း - 15 သိန်း" instead of "$115,000").

UI Rules (Gamified EdTech Style):

Remove the complicated Bar Chart at the bottom.

Layout: Use a 2-column layout. Left side is a vertical list of selectable Job Cards. Right side is a sticky detail panel for the selected job.

Styling: Use the exact Green & White Gamified style (border-2, rounded-2xl, shadow-[0_4px_0_#cbd5e1], and Emojis instead of SVGs).

The detail panel should clearly separate "Description", "Key Duties (Bulleted)", "Required Skills (Tags)", and "Starting Salary".
